#!/bin/sh

sudo add-apt-repository ppa:ubuntu-desktop/ubuntu-make -y

sudo apt update
sudo apt install ubuntu-make -y

echo "[ubuntu错误解决E: Sub-process /usr/bin/dpkg returned an error code (1) - 半叶寒羽](http://yanue.net/post-123.html)"
